import { GaAutocomplete } from './ga-autocomplete';

describe('GaAutocomplete', () => {
  it('should create an instance', () => {
    const directive = new GaAutocomplete();
    expect(directive).toBeTruthy();
  });
});
